{
    IBClasses = (
        {CLASS = DDCustomOpenGLView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                cancelOpenPanel = id; 
                endOpenPanel = id; 
                filterChanged = id; 
                hideOpenPanel = id; 
                nullAction = id; 
                raiseOpenPanel = id; 
                resizeToActualSize = id; 
                resizeToDoubleSize = id; 
                resizeToOptimalSize = id; 
                showRomLoadingLog = id; 
                togglePause = id; 
            }; 
            CLASS = MameController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mDrawer = NSDrawer; 
                mFilterButton = NSPopUpButton; 
                mGameTextField = NSComboBox; 
                mMameView = MameView; 
                mOpenPanel = NSWindow; 
                mRomLoadingLog = NSTextView; 
                mRomLoadingLogPanel = NSPanel; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = MameView; 
            LANGUAGE = ObjC; 
            OUTLETS = {mController = MameController; mDelegate = id; }; 
            SUPERCLASS = DDCustomOpenGLView; 
        }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }
    ); 
    IBVersion = 1; 
}